# PRO-03 — DWG + Professional PDF Production V0

The environment has no AutoCAD, native DWG SDK or `ezdxf`; therefore AIAS does not fabricate DWG files. It provides controlled ASCII DXF exchange files, a multipage PDF writer and an explicit export manifest marking DWG as BLOCKED and outputs as V0_LIMITED.
