# PP-09 — Pilot Building Full Project Issuance V0

`run_pp09_pilot_issuance.py` executes a fresh, versioned Venezuela pilot input through the canonical chain and writes a hashed `PROJECT_ISSUANCE_MANIFEST.json`. Outputs are classified honestly; no professional seal or unsupported format is fabricated.
