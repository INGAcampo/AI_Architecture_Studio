# PP-07 — Memories / Specifications / Reports V0

`aias_reports_core.ReportCore` generates evidence-bound JSON and Markdown documents from the canonical Project Graph, Standards Pack, Analysis Results, Drawing Model and Quantity Package. Each document carries a SHA-256 binding and the generator fails closed when required artifacts are absent.
