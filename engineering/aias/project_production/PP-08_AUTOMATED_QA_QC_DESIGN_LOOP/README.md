# PP-08 — Automated QA/QC + Design Loop V0

`aias_qa_core` implements a fail-closed Project Completeness Gate, stable SHA-256 findings, severity, evidence, audit trail and bounded deterministic correction loop. Critical blockers prevent `READY_FOR_PILOT_ISSUANCE`.
