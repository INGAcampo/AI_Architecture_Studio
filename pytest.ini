[pytest]
testpaths = tests
pythonpath = . src
addopts = -q --strict-markers --tb=short --import-mode=importlib
cache_dir = .pytest_cache
markers =
    unit: pruebas unitarias
    integration: pruebas de integración
    benchmark: pruebas de rendimiento
    slow: pruebas lentas
