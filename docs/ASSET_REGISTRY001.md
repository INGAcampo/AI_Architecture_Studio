# ASSET-REGISTRY-001

Canonical persistent registry for every AIAS engineering asset. It enforces stable identifiers, semantic versions, dependency integrity, sequential lifecycle transitions, atomic writes and append-only audit events. Assets retain requirements, evidence and legal status so inventory is operational rather than a filename list.
