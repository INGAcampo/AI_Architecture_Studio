# AIAS-NEXT-084 — Evidence review decision record

Creates an explicit decision record for each evidence item. The safe default is `PENDING_REVIEW`; only an explicit `ACCEPTED` decision sets `approved` to true.
