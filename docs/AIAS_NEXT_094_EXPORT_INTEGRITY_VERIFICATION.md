# AIAS-NEXT-094 — Export integrity verification

Computes and compares an exported artifact's SHA-256 digest. A match confirms byte-level integrity relative to the supplied digest only; it does not establish authorship or external approval.
