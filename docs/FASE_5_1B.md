# Fase 5.1B — Integración GUI

Esta entrega conecta `ProjectIO` con `MainWindow` mediante `ProjectSessionController`.
El archivo `.aias` conserva metadatos, configuración, capas, cámara y un grafo JSON de la escena.
No utiliza `pickle`; las clases se reconstruyen por módulo y nombre cualificado.
