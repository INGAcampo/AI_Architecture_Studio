# ROADMAP-RECONCILIATION-002

La reconciliación distingue una decisión aprobada de una capacidad implementada. Verifica cada ruta declarada como evidencia, conserva las dependencias externas y el diferimiento de EXP-COMMS-001, y selecciona brechas técnicas según el orden histórico aprobado.

El primer resultado detectó una ruta histórica obsoleta (`src/geometry`) y la corrigió a la autoridad real (`src/aias_geometry_kernel`). El informe válido selecciona `GEN-009`, correspondiente a internacionalización y perfiles de país, como siguiente trabajo independiente.
